import 'package:flutter/material.dart';

class SearchBus extends StatelessWidget {
  const SearchBus({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('SearchBus'),
      ),
    );
  }
}
